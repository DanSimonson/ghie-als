import React, { useState, useEffect } from "react";
import styled from "styled-components";
import { auth } from "../Firebase";

function SignedInLinks() {
  return (
    <div>
      <h1>signed out links: </h1>
    </div>
  );
}

export default SignedInLinks;
